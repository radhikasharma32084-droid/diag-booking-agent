# Copy this file to ".env" and fill in your own keys.
# Never commit the real ".env" file to GitHub.

# Optional — if not set, the agent uses simple keyword matching instead.
ANTHROPIC_API_KEY=

# Optional — if not set, the agent creates a clearly-labeled MOCK order instead.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
